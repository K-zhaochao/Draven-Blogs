# 常用方法

`Thread` 的成员方法：
![[Draven_Note_Images/Java/23.多线程/23. 多线程_常用方法：_06.png]]

![[Draven_Note_Images/Java/23.多线程/23. 多线程_常用方法：_07.png]]

##### 设置名字：

① 如果没有给线程设置名字，则线程会有默认名字：Thread-X（X序号，从0开始，每创建一个线程对象就会依次递增）；

② 如果要给线程设置名字，可以用set方法进行设置，也可以在构造方法中设置。

##### 设置优先级：

① 在Java中采用的是抢占式调度线程的执行；

② 线程的优先级越高就先执行该线程，线程优先级区间：1~10（默认为5）。

##### 守护线程：

**注意：** 为true的时候就是守护线程。
![[Draven_Note_Images/Java/23.多线程/23. 多线程_守护线程：_08.png]]



##### 出让/插入线程：

![[Draven_Note_Images/Java/23.多线程/23. 多线程_出让_插入线程：_09.png]]

# 7. 会话&拦截&异常处理

## \*登陆校验

![[../../Draven_Note_Images/JavaWeb/SpringBootWeb/7.会话&拦截&异常处理/7. 会话&拦截&异常处理_登陆校验_01.png]]

![[../../Draven_Note_Images/JavaWeb/SpringBootWeb/7.会话&拦截&异常处理/7. 会话&拦截&异常处理_登陆校验_02.png]]

## 会话技术

![[../../Draven_Note_Images/JavaWeb/SpringBootWeb/7.会话&拦截&异常处理/7. 会话&拦截&异常处理_会话技术_03.png]]

### `Cookie`方案

![[../../Draven_Note_Images/JavaWeb/SpringBootWeb/7.会话&拦截&异常处理/7. 会话&拦截&异常处理_`Cookie`方案_04.png]]

*举例（*`_Cookie_`*）：*

![[../../Draven_Note_Images/JavaWeb/SpringBootWeb/7.会话&拦截&异常处理/7. 会话&拦截&异常处理_`Cookie`方案_05.png]]

### `Session`方案

![[../../Draven_Note_Images/JavaWeb/SpringBootWeb/7.会话&拦截&异常处理/7. 会话&拦截&异常处理_`Session`方案_06.png]]

*举例（*`_Session_`*）：*

![[../../Draven_Note_Images/JavaWeb/SpringBootWeb/7.会话&拦截&异常处理/7. 会话&拦截&异常处理_`Session`方案_07.png]]

***

## 拦截技术

### `JWT`令牌

*注：JSON工具类*[*【点击品鉴】*](JavaWeb/SpringBootWeb/附录2：注解整理.md)

#### 介绍

![[../../Draven_Note_Images/JavaWeb/SpringBootWeb/7.会话&拦截&异常处理/7. 会话&拦截&异常处理_介绍_08.png]]

![[../../Draven_Note_Images/JavaWeb/SpringBootWeb/7.会话&拦截&异常处理/7. 会话&拦截&异常处理_介绍_09.png]]

![[../../Draven_Note_Images/JavaWeb/SpringBootWeb/7.会话&拦截&异常处理/7. 会话&拦截&异常处理_介绍_10.png]]

![[../../Draven_Note_Images/JavaWeb/SpringBootWeb/7.会话&拦截&异常处理/7. 会话&拦截&异常处理_介绍_11.png]]

#### 校验令牌

![[../../Draven_Note_Images/JavaWeb/SpringBootWeb/7.会话&拦截&异常处理/7. 会话&拦截&异常处理_校验令牌_12.png]]

#### 生成令牌

![[../../Draven_Note_Images/JavaWeb/SpringBootWeb/7.会话&拦截&异常处理/7. 会话&拦截&异常处理_生成令牌_13.png]]

### 过滤器`Filter`

#### 介绍

![[../../Draven_Note_Images/JavaWeb/SpringBootWeb/7.会话&拦截&异常处理/7. 会话&拦截&异常处理_介绍_14.png]]

#### 简单入门

![[../../Draven_Note_Images/JavaWeb/SpringBootWeb/7.会话&拦截&异常处理/7. 会话&拦截&异常处理_简单入门_15.png]]

#### 执行流程

![[../../Draven_Note_Images/JavaWeb/SpringBootWeb/7.会话&拦截&异常处理/7. 会话&拦截&异常处理_执行流程_16.png]]

#### 拦截路径

![[../../Draven_Note_Images/JavaWeb/SpringBootWeb/7.会话&拦截&异常处理/7. 会话&拦截&异常处理_拦截路径_17.png]]

#### 过滤器链

![[../../Draven_Note_Images/JavaWeb/SpringBootWeb/7.会话&拦截&异常处理/7. 会话&拦截&异常处理_过滤器链_18.png]]

#### 案例【登陆】

![[../../Draven_Note_Images/JavaWeb/SpringBootWeb/7.会话&拦截&异常处理/7. 会话&拦截&异常处理_案例【登陆】_19.png]]

![[../../Draven_Note_Images/JavaWeb/SpringBootWeb/7.会话&拦截&异常处理/7. 会话&拦截&异常处理_案例【登陆】_20.png]]

```java
@Slf4j
@WebFilter(urlPatterns = "/*")
public class LoginCheckFilter implements Filter {
    @Override
    public void doFilter(ServletRequest servletRequest, ServletResponse servletResponse, FilterChain filterChain) throws IOException, ServletException {
        HttpServletRequest httpServletRequest = (HttpServletRequest) servletRequest;
        HttpServletResponse httpServletResponse = (HttpServletResponse) servletResponse;
        //1.获取请求url
        String url = httpServletRequest.getRequestURL().toString();
        log.info("请求的url: {}", url);

        //2.判断请求url中是否包含login，如果包含，说明是登录操作，放行
        if (url.contains("login")) {
            log.info("登陆请求，放行...");
            filterChain.doFilter(servletRequest, servletResponse);
            return ;
        }

        //3.获取请求头中的令牌（token）
        String jwt = httpServletRequest.getHeader("token");

        //4.判断令牌是否存在，如果不存在，返回错误结果（未登录）
        if (!StringUtils.hasLength(jwt)) {
            log.info("请求头token为空，返回未登录的信息");
            Result error = Result.error("NOT_LOGIN") ;
            // 手动转换 对象--json -------------> 阿里巴巴fastJSON
            String notLogin = JSONObject.toJSONString(error);
            httpServletResponse.getWriter().write(notLogin);
            return ;
        }

        //5.解析token，如果解析失败，返回错误结果（未登录）
        try {
            JwtUtils.parseJWT(jwt);
        } catch (Exception e) { // 令牌解析失败
            log.info("解析令牌失败，返回未登录错误信息");
            Result error = Result.error("NOT_LOGIN") ;
            // 手动转换 对象--json -------------> 阿里巴巴fastJSON
            String notLogin = JSONObject.toJSONString(error);
            httpServletResponse.getWriter().write(notLogin);
            return ;
        }

        //6.放行
        log.info("令牌合法，放行");
        filterChain.doFilter(servletRequest, servletResponse);
    }
}
```

### 拦截器`Interceptor`

#### 介绍

![[../../Draven_Note_Images/JavaWeb/SpringBootWeb/7.会话&拦截&异常处理/7. 会话&拦截&异常处理_介绍_21.png]]

#### 简单入门

![[../../Draven_Note_Images/JavaWeb/SpringBootWeb/7.会话&拦截&异常处理/7. 会话&拦截&异常处理_简单入门_22.png]]

#### 拦截路径

![[../../Draven_Note_Images/JavaWeb/SpringBootWeb/7.会话&拦截&异常处理/7. 会话&拦截&异常处理_拦截路径_23.png]]

#### 执行流程

![[../../Draven_Note_Images/JavaWeb/SpringBootWeb/7.会话&拦截&异常处理/7. 会话&拦截&异常处理_执行流程_24.png]]

#### 案例【登陆】

![[../../Draven_Note_Images/JavaWeb/SpringBootWeb/7.会话&拦截&异常处理/7. 会话&拦截&异常处理_案例【登陆】_25.png]]

```java
@Slf4j
@Component
public class LoginCheckInterceptor implements HandlerInterceptor {
    @Override //目标资源方法执行前执行，返回true放行，返回false不放心
    public boolean preHandle(HttpServletRequest httpServletRequest, HttpServletResponse httpServletResponse, Object handler) throws Exception {
        System.out.println("preHandle...");
        //1.获取请求url
        String url = httpServletRequest.getRequestURL().toString();
        log.info("请求的url: {}", url);

        //2.获取请求头中的令牌（token）
        String jwt = httpServletRequest.getHeader("token");

        //3.判断令牌是否存在，如果不存在，返回错误结果（未登录）
        if (!StringUtils.hasLength(jwt)) {
            log.info("请求头token为空，返回未登录的信息");
            Result error = Result.error("NOT_LOGIN") ;
            // 手动转换 对象--json -------------> 阿里巴巴fastJSON
            String notLogin = JSONObject.toJSONString(error);
            httpServletResponse.getWriter().write(notLogin);
            return false;
        }

        //4.解析token，如果解析失败，返回错误结果（未登录）
        try {
            JwtUtils.parseJWT(jwt);
        } catch (Exception e) { // 令牌解析失败
            log.info("解析令牌失败，返回未登录错误信息");
            Result error = Result.error("NOT_LOGIN") ;
            // 手动转换 对象--json -------------> 阿里巴巴fastJSON
            String notLogin = JSONObject.toJSONString(error);
            httpServletResponse.getWriter().write(notLogin);
            return false;
        }

        //5.放行
        log.info("令牌合法，放行");
        return true;
    }

    @Override //目标资源方法执行后执行
    public void postHandle(HttpServletRequest request, HttpServletResponse response, Object handler, @Nullable ModelAndView modelAndView) throws Exception {
        HandlerInterceptor.super.postHandle(request, response, handler, modelAndView);
    }

    @Override //视图渲染完毕后执行，最后执行
    public void afterCompletion(HttpServletRequest request, HttpServletResponse response, Object handler, @Nullable Exception ex) throws Exception {
        HandlerInterceptor.super.afterCompletion(request, response, handler, ex);
    }
}
```

```java
@Configuration
public class WebConfig implements WebMvcConfigurer {
    @Autowired
    private LoginCheckInterceptor loginCheckInterceptor;

    @Override
    public void addInterceptors(InterceptorRegistry registry) {
        InterceptorRegistration interceptor = registry.addInterceptor(loginCheckInterceptor).addPathPatterns("/**");
        // 判断请求url中是否包含login，如果包含，说明是登录操作，放行
        interceptor.excludePathPatterns("/login");
    }
}
```

## 异常处理

![[../../Draven_Note_Images/JavaWeb/SpringBootWeb/7.会话&拦截&异常处理/7. 会话&拦截&异常处理_异常处理_26.png]]

![[../../Draven_Note_Images/JavaWeb/SpringBootWeb/7.会话&拦截&异常处理/7. 会话&拦截&异常处理_异常处理_27.png]]

![[../../Draven_Note_Images/JavaWeb/SpringBootWeb/7.会话&拦截&异常处理/7. 会话&拦截&异常处理_异常处理_28.png]]

```java
@RestControllerAdvice
public class GlobalExceptionHandler {
    @ExceptionHandler(Exception.class)
    public Result ex(Exception ex) {
        ex.printStackTrace();;
        return Result.error("对不起, 操作失败, 请联系管理人员");
    }
}
```

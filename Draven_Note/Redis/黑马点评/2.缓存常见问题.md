---
title: 2.缓存常见问题
---
# 缓存常见问题

### 缓存概念：

![[Draven_Note_Images/Pasted image 20260603195219.png]]

![[Draven_Note_Images/Pasted image 20260603195405.png]]

### Redis缓存：

![[Draven_Note_Images/Pasted image 20260603195740.png]]

#### 代码实现：

``` java
public Result queryById(Long id) {  
    String key = RedisConstants.CACHE_SHOP_KEY + id;  
    // 1.从redis查询商铺缓存  
    String shopJson = stringRedisTemplate.opsForValue().get(key);  
  
    // 2.判断是否存在  
    if (StrUtil.isNotBlank(shopJson)) {  
        // 3.存在，直接返回  
        Shop shop = JSONUtil.toBean(shopJson, Shop.class);  
        return Result.ok(shop);  
    }  
  
    // 4.不存在，根据id查询数据库  
    Shop shop = getById(id);  
  
    if (shop == null) {  
        // 5.不存在，返回错误  
        return Result.fail("店铺不存在！");  
    }  
  
    // 6.存在，写入redis  
    stringRedisTemplate.opsForValue().set(key, JSONUtil.toJsonStr(shop));  
  
    // 7.返回  
    return Result.ok(shop);  
}
```

#### 练习：

![[Draven_Note_Images/Pasted image 20260603204130.png]]

``` java
public Result queryShopTypeList() {  
    String key = RedisConstants.CACHE_SHOP_TYPE_KEY;  
    // 1.从Redis获取商户类型信息  
    String shopTypeListJson = stringRedisTemplate.opsForValue().get(key);  
  
    // 2.若存在，则直接返回  
    if (shopTypeListJson != null && !shopTypeListJson.isEmpty()) {  
        try {  
            List<ShopType> shopTypeList = objectMapper  
                    .readValue(shopTypeListJson, new TypeReference<List<ShopType>>() {});  
            return Result.ok(shopTypeList);  
        } catch (JsonProcessingException e) {  
            throw new RuntimeException("缓存解析失败", e);  
        }  
    }  
  
    // 3.若不存在则去数据库查找  
    List<ShopType> shopTypeList = query().orderByAsc("sort").list();  
  
    // 4.若数据库不存在则返回错误  
    if (shopTypeList == null || shopTypeList.isEmpty()) {  
        return Result.fail("商品分类不存在！");  
    }  
  
    // 5.将商户类型信息存入Redis  
    try {  
        shopTypeListJson = objectMapper.writeValueAsString(shopTypeList);  
        stringRedisTemplate.opsForValue().set(key, shopTypeListJson, RedisConstants.SHOP_TYPE_TTL, TimeUnit.MINUTES);  
    } catch (JsonProcessingException e) {  
        e.printStackTrace();  
    }  
  
    // 6.返回商户信息  
    return Result.ok(shopTypeList);  
}
```

### 缓存更新策略：

#### 缓存机制对比：

![[Draven_Note_Images/Pasted image 20260604112855.png]]

![[Draven_Note_Images/Pasted image 20260604112743.png]]

#### 高效缓存机制：

![[Draven_Note_Images/Pasted image 20260604112748.png]]

#### 线程执行异常问题：

**异常1：** 先删缓存数据，再操作数据库填入数据【可能性高】
线程1删掉缓存，线程2执行读取，数据库将未更新的数据填入缓存，线程1再完成数据库更新，此时数据库与缓存的值出现差异。
![[Draven_Note_Images/Pasted image 20260604113328.png]]

**异常2：** 先操作数据库填入数据，再删除缓存【可能性低】
恰好线程2未操作数据库之前缓存失效，且此时线程1来查询数据，线程1就会直接读取数据库，而线程2进行了操作数据库和删除缓存之后线程1就将读取到的旧数据添加到了缓存。
![[Draven_Note_Images/Pasted image 20260604114059.png]]

#### 总结：

![[Draven_Note_Images/Pasted image 20260604114226.png]]

### 缓存穿透：

#### 概念&解决方案：

![[Draven_Note_Images/Pasted image 20260607122026.png]]

#### 实现思路：

![[Draven_Note_Images/Pasted image 20260607122123.png]]

#### 代码实现：

``` java
public Result queryById(Long id) {  
    String key = RedisConstants.CACHE_SHOP_KEY + id;  
    // 1.从redis查询商铺缓存  
    String shopJson = stringRedisTemplate.opsForValue().get(key);  
  
    // 2.判断是否存在  
    if (StrUtil.isNotBlank(shopJson)) {  
        // 3.存在，直接返回  
        Shop shop = JSONUtil.toBean(shopJson, Shop.class);  
        return Result.ok(shop);  
    }  
  
    // 判断命中是否是空值  
    // 前面已经判断了shopJson是否存在, 既然不存在那么shopJson必定  
    // 是null, 如果不是null就表示该数据是"", 防穿透的数据  
    if (shopJson != null) {  
        return Result.fail("店铺信息不存在！");  
    }  
  
    // 4.不存在，根据id查询数据库  
    Shop shop = getById(id);  
  
    if (shop == null) {  
        // 5.不存在，返回错误  
        stringRedisTemplate.opsForValue().set(key, "", RedisConstants.CACHE_NULL_TTL, TimeUnit.MINUTES);  
        return Result.fail("店铺不存在！");  
    }  
  
    // 6.存在，写入redis  
    stringRedisTemplate.opsForValue().set(key, JSONUtil.toJsonStr(shop), RedisConstants.CACHE_SHOP_TTL, TimeUnit.MINUTES);  
  
    // 7.返回  
    return Result.ok(shop);  
}
```

#### 总结：

![[Draven_Note_Images/Pasted image 20260608093514.png]]

### 缓存雪崩：

#### 概念：

![[Draven_Note_Images/Pasted image 20260608094300.png]]

### 缓存击穿：

#### 概念：

![[Draven_Note_Images/Pasted image 20260608094728.png]]

#### 解决方案：

![[Draven_Note_Images/Pasted image 20260608095250.png]]

#### 方案对比：

![[Draven_Note_Images/Pasted image 20260608095345.png]]

#### 案例1：互斥锁

![[Draven_Note_Images/Pasted image 20260608165441.png]]

``` java
public Shop queryWithMutex(Long id) {  
    String key = RedisConstants.CACHE_SHOP_KEY + id;  
    // 1.从redis查询商铺缓存  
    String shopJson = stringRedisTemplate.opsForValue().get(key);  
  
    // 2.判断是否存在  
    if (StrUtil.isNotBlank(shopJson)) {  
        // 3.存在，直接返回  
        return JSONUtil.toBean(shopJson, Shop.class);  
    }  
  
    // 判断命中是否是空值  
    // 前面已经判断了shopJson是否存在, 既然不存在那么shopJson必定  
    // 是null, 如果不是null就表示该数据是"", 防穿透的数据  
    if (shopJson != null) {  
        return null;  
    }  
  
    // 4.实现缓存重建  
    // 4.1.尝试获取互斥锁  
    Shop shop = null;  
    try {  
        boolean isLock = tryLock(RedisConstants.LOCK_SHOP_KEY + id);  
  
        // 4.2.若获取锁失败，则休息一段时间  
        if (!isLock) {  
            while (true) {  
                Thread.sleep(50);  
                // 1.从redis查询商铺缓存  
                shopJson = stringRedisTemplate.opsForValue().get(key);  
                // 2.判断是否存在  
                if (StrUtil.isNotBlank(shopJson)) {  
                    // 3.存在，直接返回  
                    return JSONUtil.toBean(shopJson, Shop.class);  
                }  
                // 判断命中是否是空值  
                // 前面已经判断了shopJson是否存在, 既然不存在那么shopJson必定  
                // 是null, 如果不是null就表示该数据是"", 防穿透的数据  
                if (shopJson != null) {  
                    return null;  
                }  
            }  
        }  
  
        // 4.3.若获取锁成功则进行SQL查询  
        Thread.sleep(200);  
        shop = getById(id);  
  
        if (shop == null) {  
            // 5.查询不存在，缓存穿透保障  
            stringRedisTemplate.opsForValue().set(key, "", RedisConstants.CACHE_NULL_TTL, TimeUnit.MINUTES);  
            return null;  
        }  
  
        // 6.存在，写入redis  
        stringRedisTemplate.opsForValue().set(key, JSONUtil.toJsonStr(shop), RedisConstants.CACHE_SHOP_TTL, TimeUnit.MINUTES);  
    } catch (InterruptedException e) {  
        throw new RuntimeException(e);  
    } finally {  
        // 7.关闭互斥锁  
        unlock(RedisConstants.LOCK_SHOP_KEY + id);  
    }  
  
    // 8.返回数据  
    return shop;  
}
```

#### 案例2：逻辑过期
**注意：** 默认热点信息是提前加载到Redis中，因此若未命中则代表无该信息返回 `null` 。

![[Draven_Note_Images/Pasted image 20260608214624.png]]

``` java
// 1.先创建一个用于存储带有过期时间的对象
@Data  
public class RedisData {  
    private LocalDateTime expireTime;  
    private Object data;  
}
```

``` java
// 2.逻辑过期的实现

```

``` java
// 在测试中方便手动对Redis进行预热
public void saveshop2Redis(Long id, Long expireSeconds) {  
    // 1.查询店铺数据  
    Shop shop = getById(id);  
    // 2.封装逻辑过期时间  
    RedisData redisData = new RedisData();  
    redisData.setData(shop);  
    redisData.setExpireTime(LocalDateTime.now().plusSeconds(expireSeconds));  
    // 3.写入Redis  
    stringRedisTemplate.opsForValue().set(RedisConstants.CACHE_SHOP_KEY + id, JSONUtil.toJsonStr(redisData));  
}
```
### 缓存封装工具


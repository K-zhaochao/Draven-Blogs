---
title: 5.秒杀相关问题
---
# 秒杀相关问题

## 全局唯一ID

### 概念：

![[Draven_Note_Images/Pasted image 20260609170126.png]]

![[Draven_Note_Images/Pasted image 20260609171658.png]]

![[Draven_Note_Images/Pasted image 20260609171701.png]]

### 代码实现：

``` java
@Component  
public class RedisIdWorker {  
  
    // 开始时间戳  
    private static final long BEGIN_TIMESTAMP = 1640995200L;  
    // 序列号的位数  
    private static final int COUNT_BITS = 32;  
  
    @Resource  
    private StringRedisTemplate stringRedisTemplate;  
  
    public long nextId(String keyPrefix) {  
        // 1.生成时间戳  
        LocalDateTime now = LocalDateTime.now();  
        long nowSecond = now.toEpochSecond(ZoneOffset.UTC);  
        long tiemstamp = nowSecond - BEGIN_TIMESTAMP;  
  
        // 2.生成序列号  
        // 2.1.获取当前日期，精确到天  
        String date = now.format(DateTimeFormatter.ofPattern("yyyy:MM:dd"));  
        // 2.2.自增长  
        Long count = stringRedisTemplate.opsForValue().increment("icr:" + keyPrefix + ":" + date);  
  
        // 3.拼接并返回  
        return tiemstamp << COUNT_BITS | count;  
    }  
}
```

测试代码
``` java
@Resource  
private RedisIdWorker redisIdWorker;  
  
private ExecutorService executorService = Executors.newFixedThreadPool(500);

@Test  
void testIdWorker() throws InterruptedException {  
    CountDownLatch countDownLatch = new CountDownLatch(300);  
  
    Runnable runnable = () -> {  
        for (int i = 0; i < 100; i++) {  
            long orderId = redisIdWorker.nextId("order");  
            System.out.println(orderId);  
        }  
        countDownLatch.countDown();  
    };  
  
    long begain = System.currentTimeMillis();  
    for (int i = 0; i < 300; i++) {  
        executorService.submit(runnable);  
    }  
    countDownLatch.await();  
    long end = System.currentTimeMillis();  
    System.out.println("耗时: " + (end - begain) + "微秒");  
}
```

**新知识：** `CountDownLatch` 类的核心作用是**让主线程等待一组子线程全部执行完成后，再继续往下执行**。参数 `count = 300` 代表**初始计数器**，表示需要完成的任务 / 线程总数（计数器只能递减，不能重置，且计数器归 0 时，所有阻塞等待的线程会被唤醒）

### 总结：

![[Draven_Note_Images/Pasted image 20260610155656.png]]

## 实现优惠券秒杀下单



## 超卖问题


## 一人一单


## 分布式锁


## Redis优化秒杀


## Redis消息队列实现异步秒杀
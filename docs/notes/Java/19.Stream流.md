---
order: 19
title: "19.Stream流"
---

# 19. Stream流

### 作用与步骤

<img src="/Draven_Note_Images/Java/19.Stream流/19. Stream流_作用与步骤_01.png" alt="img" />

#### 获取流水线

<img src="/Draven_Note_Images/Java/19.Stream流/19. Stream流_获取流水线_02.png" alt="img" />

***注意：***

1. *双列集合可以用*`keySet()`*或*`valueSet()`*方法获取到对应键或值的单列集合再使用单列集合的获取方法来获取流水线。*
2. *如果*`Stream.of()`*传入的参数是一个数组且该数组中存入的数据是基本数据类型都话，那么传过去的就是该数组的地址而不是数据。*

#### 中间方法

<img src="/Draven_Note_Images/Java/19.Stream流/19. Stream流_中间方法_03.png" alt="img" />

**注意3：**distinct方法底层是用HashSet进行去重的，因此如果集合中的元素是自己写的对象都话，需要重写HashCode和equals方法。

**注意4：**concat中的两个参数如果类型不同都话，会将它们合并成它们共同的父类，合并之后就无法调用子类的成员了。

#### 终结方法

<img src="/Draven_Note_Images/Java/19.Stream流/19. Stream流_终结方法_04.png" alt="img" />

***注意：**\_\_如果要将最终的数据收集到Map集合里面，那么数据中不能有重复的键。*

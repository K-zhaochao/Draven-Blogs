---
order: 4
title: "4.Java中使用"
---

# Java中使用

## `Redis`的Java客户端：

![img](/Draven_Note_Images/苍穹外卖/5.Redis/5.%20Redis_`Redis`的Java客户端：_14.png)

![img](/Draven_Note_Images/Pasted%20image%2020260601172043.png)

## `Jedis` ：

#### 快速入门：

> `Jedis` 的官地址：https://github.com/redis/jedis，快速入门如下。
1. 引入依赖
``` xml
<dependency>
    <groupId>redis.clients</groupId>
    <artifactId>jedis</artifactId>
    <version>7.1.0</version>
</dependency>
```
2. 建立连接
``` java
@BeforeEach  
void setUp() {  
    // 建立连接  
    jedis = new Jedis("localhost", 6379);  
    // 设置密码  
    jedis.auth("password");  
    // 选着库  
    jedis.select(0);  
}
```
3. 测试String
``` java
@Test  
void testString() {  
    // 插入数据，方法名称就是redis命令名称  
    String result = jedis.set("name","张三");  
    System.out.println("result = " + result);  
    // 获取数据  
    String name = jedis.get("name");  
    System.out.println("name = " + name);  
}
```
4. 释放资源
``` java
@AfterEach  
void tearDown() {  
    // 释放资源  
    if (jedis != null) {  
        jedis.close();  
    }  
}
```

### `Jedis` 连接池：

![img](/Draven_Note_Images/Pasted%20image%2020260601175003.png)

## `Spring-Data-Redis` ：

### 介绍：

![img](/Draven_Note_Images/Pasted%20image%2020260601175731.png)

### 使用方式：

![img](/Draven_Note_Images/Pasted%20image%2020260601175956.png)

### 快速入门：

![img](/Draven_Note_Images/Pasted%20image%2020260601180156.png)

![img](/Draven_Note_Images/Pasted%20image%2020260601180219.png)

**注意：** `Spring` 默认使用的是lettuce，若要使用jedis都话则需要在依赖中引入jedis依赖。

![img](/Draven_Note_Images/Pasted%20image%2020260601180248.png)

**注意：** 若不用 `Redis` 的连接池，则需要自定义配置类生成 `RedisTemplate` 。

![img](/Draven_Note_Images/苍穹外卖/5.Redis/5.%20Redis_`Spring-Data-Redis`使_17.png)

### 序列化方式：

![img](/Draven_Note_Images/Pasted%20image%2020260601203409.png)

![img](/Draven_Note_Images/Pasted%20image%2020260601203349.png)

**注意：** 此处的返回值的泛指类型要和注入时候的类型一致，若没有引入 `Spring MVC` 则需要引入下方的依赖【Jackson依赖】：

``` xml
<dependency>  
    <groupId>com.fasterxml.jackson.core</groupId>  
    <artifactId>jackson-databind</artifactId>  
</dependency>
```

#### Q：怎么解决JSON序列化占用更多空间的问题？

![img](/Draven_Note_Images/Pasted%20image%2020260601205453.png)

![img](/Draven_Note_Images/Pasted%20image%2020260601205605.png)

![img](/Draven_Note_Images/Pasted%20image%2020260601205622.png)

**注意：** `ObjectMapper` 和`FastJSON` 一样都是操作类转JSON或JSON转类的工具类。

#### 总结：

![img](/Draven_Note_Images/Pasted%20image%2020260601211640.png)

### 操作案例：

#### 操作字符串类型的数据：

```java
/**
 * 操作字符串类型的数据
 */
@Test
public void testString() {
    // set get setex setnx
    redisTemplate.opsForValue().set("name","小明");

    String city =(String) redisTemplate.opsForValue().get("name");
    System.out.println(city);

    redisTemplate.opsForValue().set("code", "1234",  3, TimeUnit.MINUTES);

    redisTemplate.opsForValue().setIfAbsent("lock","1");
    redisTemplate.opsForValue().setIfAbsent("lock","2");
}
```

#### 操作哈希类型的数据：

```java
/**
 * 操作哈希类型的数据
 */
@Test
public void testHash(){
    // hset hget hdel hkeys hvals
    HashOperations hashOperations = redisTemplate.opsForHash();

    hashOperations.put("100", "name", "tom");
    hashOperations.put("100", "age", "20");

    String name = (String) hashOperations.get("100", "name");
    System.out.println(name);

    Set keys = hashOperations.keys("100");
    System.out.println(keys);

    List values = hashOperations.values("100");
    System.out.println(values);

    hashOperations.delete("100", "age");
}
```

#### 操作列表类型的数据：

```java
/**
 * 操作列表类型的数据
 */
@Test
public void testList() {
    //Lpush Lrange rpop Llen
    ListOperations listOperations = redisTemplate.opsForList();

    listOperations.leftPushAll("mylist", "a", "b", "c");
    listOperations.leftPush("mylist", "d");

    List mylist = listOperations.range("mylist", 0, -1);
    System.out.println(mylist);

    listOperations.rightPop("mylist");

    Long size = listOperations.size("mylist");
    System.out.println(size);
}
```

#### 操作集合类型的数据：

```java
/**
 * 操作集合类型的数据
 */
@Test
public void testSet() {
    // sadd smembers scard sinter sunion srem
    SetOperations setOperations = redisTemplate.opsForSet();

    setOperations.add("set1", "a", "b", "c", "d");
    setOperations.add("set2", "a", "b", "x", "y");

    Set members = setOperations.members("set1");
    System.out.println(members);

    Long size = setOperations.size("set1");
    System.out.println(size);

    Set intersect = setOperations.intersect("set1", "set2");
    System.out.println(intersect);

    Set union = setOperations.union("set1", "set2");
    System.out.println(union);

    setOperations.remove("set1", "a", "b");
}
```

#### 操作有序集合类型的数据：

```java
/**
 * 操作有序集合类型的数据
 */
@Test
public void testZset() {
    //Zadd zrange zincrby zrem
    ZSetOperations zSetOperations = redisTemplate.opsForZSet();

    zSetOperations.add("zset1", "a", 10);
    zSetOperations.add("zset1", "b", 12);
    zSetOperations.add("zset1", "c", 9);

    Set zset1 = zSetOperations.range("zset1", 0, -1);
    System.out.println(zset1);

    zSetOperations.incrementScore("zset1", "c", 10);

    zSetOperations.remove("zset1", "a", "b");
}
```

#### 通用命令操作

```java
/**
 * 通用命令操作
 */
@Test
public void testCommon(){
    //keys exists type deL
    Set keys = redisTemplate.keys("*");
    System.out.println(keys);

    Boolean name = redisTemplate.hasKey("name");
    Boolean set1 = redisTemplate.hasKey("set1");

    for (Object key : keys) {
        DataType type = redisTemplate.type(key);
        System.out.println(type.name());
    }

    redisTemplate.delete("mylist");
}
```

---
order: 1
title: "1.短信登陆"

---
# 短信登陆

### 基于 `Session` 实现登录

#### 实现逻辑：
![img](/Draven_Note_Images/Pasted%20image%2020260602115512.png)

#### 发送短信验证码：

![img](/Draven_Note_Images/Pasted%20image%2020260602162853.png)

**代码实现：** 
``` java
public Result sendCode(String phone, HttpSession session) {  
    // 1.校验手机号  
    if (!RegexUtils.isPhoneInvalid(phone)){  
        // 2.如果不符合，返回错误信息  
        return Result.fail("手机号格式错误！");  
    }  
  
    // 3.符合，生成验证码  
    String code = RandomUtil.randomNumbers(6);  
  
    // 4。保存验证码到session  
    session.setAttribute("code", code);  
  
    // 5.发送验证码  
    log.debug("发送短信验证码成功, 验证码: {}", code);  
  
    // 返回ok  
    return Result.ok();  
}
```

#### 短信验证码登陆：

![img](/Draven_Note_Images/Pasted%20image%2020260602164218.png)
**代码实现：**
``` java

```

### 集群的 `session` 共享问题

### 基于Redis实现共享 `session` 登录
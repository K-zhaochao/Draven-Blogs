---
order: 8
title: "8.缓存商品&购物车"
---

# 8. 缓存商品&购物车

![img](/Draven_Note_Images/苍穹外卖/8.缓存商品&购物车/8.%20缓存商品&购物车_01.png)

### 缓存菜品

#### 问题说明：

![img](/Draven_Note_Images/苍穹外卖/8.缓存商品&购物车/8.%20缓存商品&购物车_问题说明：_02.png)

#### 实现思路：

![img](/Draven_Note_Images/苍穹外卖/8.缓存商品&购物车/8.%20缓存商品&购物车_实现思路：_03.png)

![img](/Draven_Note_Images/苍穹外卖/8.缓存商品&购物车/8.%20缓存商品&购物车_实现思路：_04.png)

#### 代码开发：

``` java
public Result<List&lt;DishVO&gt;> list(Long categoryId) {  
    // 从Redis缓存中获取  
    String key = "dish_" + categoryId;  
    List&lt;DishVO&gt; list = (List&lt;DishVO&gt;) redisTemplate.opsForValue().get(key);  
    if (list != null && !list.isEmpty()) {  
        return Result.success(list);  
    }  
  
    Dish dish = new Dish();  
    dish.setCategoryId(categoryId);  
    dish.setStatus(StatusConstant.ENABLE);//查询起售中的菜品  
  
    list = dishService.listWithFlavor(dish);  
    // 更新Redis中的数据  
    redisTemplate.opsForValue().set(key, list);  
  
    return Result.success(list);  
}
```

### 缓存套餐

#### `Spring Cache`：

![img](/Draven_Note_Images/Pasted%20image%2020260510162421.png)

![img](/Draven_Note_Images/028043714292599a226a80d8ab6e12c7.png)

![img](/Draven_Note_Images/176f2b9efc94150896f44f0f83a3e10d.png)

**注意：** 若希望用 `@CacheEvict` 删除指定目录的所有数据，只需要将 `allEntries` 属性设置为 `true` 即可。

#### 实现思路：

![img](/Draven_Note_Images/Pasted%20image%2020260511173154.png)

### 添加购物车

#### 需求分析与设计：

![img](/Draven_Note_Images/Pasted%20image%2020260511195116.png)

![img](/Draven_Note_Images/Pasted%20image%2020260511195213.png)

![img](/Draven_Note_Images/Pasted%20image%2020260511195421.png)

![img](/Draven_Note_Images/Pasted%20image%2020260512143807.png)

#### 代码开发：

``` java
public void addShoppingCart(ShoppingCartDTO shoppingCartDTO) {  
    // 判断当前架入到购物车中的商品是否已经存在了  
    ShoppingCart shoppingCart = new ShoppingCart();  
    BeanUtils.copyProperties(shoppingCartDTO, shoppingCart);  
  
    Long userId = BaseContext.getCurrentId();  
    shoppingCart.setUserId(userId);  
  
    List&lt;ShoppingCart&gt; list = shoppingCartMapper.list(shoppingCart);  
  
    // 如果已经存在了，只需要将数量加一  
    if (list != null && !list.isEmpty()) {  
        ShoppingCart cart = list.get(0);  
        Integer number = cart.getNumber() + 1;  
        cart.setNumber(number);  
        shoppingCartMapper.updateNumberById(cart);  
    } else {  
        // 如果不存在，需要插入一条购物车数据  
        Long dishId = shoppingCartDTO.getDishId();  
        if (dishId != null) {  
            // 本次添加到购物车的是菜品  
            Dish dish = dishMapper.getById(dishId);  
            shoppingCart.setName(dish.getName());  
            shoppingCart.setImage(dish.getImage());  
            shoppingCart.setAmount(dish.getPrice());  
        } else {  
            // 本次添加到购物车的是套餐  
            Long setmealId = shoppingCart.getSetmealId();  
  
            Setmeal setmeal = setmealMapper.getById(setmealId);  
            shoppingCart.setName(setmeal.getName());  
            shoppingCart.setImage(setmeal.getImage());  
            shoppingCart.setAmount(setmeal.getPrice());  
        }  
        shoppingCart.setNumber(1);  
        shoppingCart.setCreateTime(LocalDateTime.now());  
        shoppingCartMapper.insert(shoppingCart);  
    }  
}
```

### 查看购物车

#### 需求分析与设计：

![img](/Draven_Note_Images/Pasted%20image%2020260512193905.png)

![img](/Draven_Note_Images/Pasted%20image%2020260512193920.png)

``` java
public List&lt;ShoppingCart&gt; showShoppingCart() {  
    Long userId = BaseContext.getCurrentId();  
    ShoppingCart shoppingCart = ShoppingCart.builder()  
            .userId(userId)  
            .build();  
    return shoppingCartMapper.list(shoppingCart);  
}
```

### 清空购物车

#### 需求分析与设计：

![img](/Draven_Note_Images/Pasted%20image%2020260513102854.png)

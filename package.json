{
  "devDependencies": {
    "chokidar": "^5.0.0",
    "markdown-it-task-lists": "^2.1.1",
    "medium-zoom": "^1.1.0",
    "vitepress": "^1.6.4",
    "vitepress-sidebar": "^1.33.2",
    "vue": "^3.5.33"
  },
  "scripts": {
    "sync": "node scripts/sync.mjs",
    "dev": "node scripts/sync.mjs && vitepress dev docs",
    "build": "node scripts/sync.mjs && vitepress build docs",
    "preview": "vitepress preview docs",
    "watch": "node scripts/sync.mjs --watch"
  }
}
